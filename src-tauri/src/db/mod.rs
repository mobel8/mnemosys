//! Database layer (SQLite via rusqlite).
//!
//! TODO(A2): filled by agent A2 — Database Engineer.
//! Expected contents:
//! - `Connection` pool / managed state.
//! - Schema migrations.
//! - Repositories for decks, cards, reviews, settings.
//!
//! Public re-exports MUST keep `pub use` so downstream agents only need
//! `use crate::db::*` in command modules.
