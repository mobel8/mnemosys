//! Tauri `#[command]` handlers — front-end facing API surface.
//!
//! TODO(B1+): filled by Wave-B agents.
//! Each feature should declare a sub-module here (e.g. `deck`, `card`,
//! `review`, `import`, `stats`) and re-export the handlers through this
//! module so `lib.rs::run()` only needs `commands::deck::list_decks` etc.
